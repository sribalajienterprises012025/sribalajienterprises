# Cloudflare Pages: serve index.html for every path so React Router can handle
# deep links. Without this, opening /trips directly returns a 404 on a cold
# visit — before the service worker is installed and able to answer navigations.
/*  /index.html  200
