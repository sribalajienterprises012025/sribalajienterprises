# dependencies
node_modules
.pnp
.pnp.js

# build output
dist
*.tsbuildinfo
dist-ssr
build
*.local

# environment
# .env.production IS committed: it holds only the project URL and publishable
# key, both of which ship in the browser bundle anyway. Anything secret belongs
# in .env.local (never committed) or in the deploy platform.
.env
.env.local
.env.*.local

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# supabase local
supabase/.branches
supabase/.temp
.supabase

# generated PWA assets
dev-dist
